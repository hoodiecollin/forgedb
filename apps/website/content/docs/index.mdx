---
title: "What is ForgeDB"
description: "An application-database generator — one schema compiles into a tailored Rust database, a TypeScript SDK, a REST API, and an OpenAPI spec."
purpose: "orientation"
---
ForgeDB is an **application-database generator**. You write one declarative `.forge`
schema, and ForgeDB compiles it into Rust database code, a TypeScript SDK, a REST API,
and an OpenAPI spec — each tailored to your schema.

```forge
User {
  id: +uuid
  name: string @length(1, 100)
  email: &string @email
  created_at: +timestamp
  posts: [Post]
}

Post {
  id: +uuid
  title: string @length(1, 200)
  views: ^u64
  published: bool
  author: *User
  created_at: +timestamp
}
```

<Callout type="note" title="A generator, not a runtime ORM">
The app's schema is a *compile-time input to generation*, never a *runtime input to a
generic engine*. Every schema-specific surface — types, tables, queries, filters,
relations, API routes — is generated and tailored per app.
</Callout>

## The generation pipeline

One schema produces four artifacts:

- **`database.rs`** — a tailored Rust database over columnar storage
- **`types.ts`** — TypeScript types + a typed client SDK
- **`api.rs`** — a REST API with an OpenAPI document
- **stubs/README** — a placeholder README for computed-field / component implementations you write yourself (no UI or component code is generated)

<DiveDeeper summary="how one schema becomes every artifact">

The schema runs through a fixed pipeline. The parser lexes it into an AST, validation
checks it, then codegen runs one generator per artifact: `RustGenerator` → `database.rs`,
`TypeScriptGenerator` → `types.ts`, `ApiGenerator` → `api.rs`, and `OpenApiGenerator` for
the spec. Two opt-in generators add more surface — `WasmGenerator` for a browser
read-replica and `TransformGenerator` for an offline migration bin.

Each generator reads the same validated schema and emits code specialized to it. None of
them ship inside your app, and none read the `.forge` at runtime — the tailored logic is
baked in at compile time. Your generated code links only schema-agnostic substrate crates
(storage, WAL, types), which know nothing about any specific schema.

</DiveDeeper>

Read on for [installation](/docs/installation/) and the [quickstart](/docs/quickstart/).
