---
title: "Installation"
description: "Install the forgedb CLI via cargo, prebuilt binaries, or from source — plus the Rust toolchain requirement and the substrate crate version matrix."
purpose: "reference"
---

ForgeDB ships as a single CLI binary, `forgedb`. Pick whichever install method fits —
they all give you the same tool.

<Callout type="note" title="Rust toolchain requirement">
Building from source (`cargo install`, `--git`, or from a clone) needs a Rust toolchain
**≥ 1.85**, since ForgeDB uses **edition 2024**. Prebuilt binaries need no toolchain.
</Callout>

## 1. `cargo install` (from crates.io)

The canonical path if you have a Rust toolchain:

```bash
cargo install forgedb
```

This builds the CLI from the published crates and drops `forgedb` in `~/.cargo/bin`.
Update later with:

```bash
cargo install forgedb --force
```

## 2. Prebuilt binaries (no toolchain needed)

Each tagged release attaches prebuilt binaries to its
[GitHub Release](https://github.com/hoodiecollin/forgedb/releases). Download the archive
for your platform, extract, and put `forgedb` on your `PATH`:

| Platform | Asset |
|---|---|
| Linux x86_64 | `forgedb-<version>-x86_64-unknown-linux-gnu.tar.gz` |
| Linux aarch64 | `forgedb-<version>-aarch64-unknown-linux-gnu.tar.gz` |
| macOS (Intel) | `forgedb-<version>-x86_64-apple-darwin.tar.gz` |
| macOS (Apple Silicon) | `forgedb-<version>-aarch64-apple-darwin.tar.gz` |
| Windows x86_64 | `forgedb-<version>-x86_64-pc-windows-msvc.zip` |

```bash
tar -xzf forgedb-<version>-<target>.tar.gz
sudo mv forgedb /usr/local/bin/     # or anywhere on your PATH
forgedb --help
```

<Callout type="warning" title="Release workflow not yet exercised">
The cross-platform release workflow is authored and validated but not yet triggered by a
real tag push. If a prebuilt asset isn't available for your platform yet, use
`cargo install` instead.
</Callout>

## 3. `cargo install --git` (latest from source)

To track the tip of `main` (or before a release is cut):

```bash
cargo install --git https://github.com/hoodiecollin/forgedb forgedb
```

This builds the whole workspace from source, so no crates need to be published first.

## 4. Build from a clone

```bash
git clone https://github.com/hoodiecollin/forgedb
cd forgedb
cargo build --release          # binary at target/release/forgedb
cargo install --path .         # or install it onto your PATH
```

## Substrate crate versions

A ForgeDB-generated app is **not** dependency-free: its generated Rust code links a set of
small, **schema-agnostic** runtime crates published on crates.io (the "substrate").
`forgedb init` pins these in the generated `Cargo.toml`, so you normally don't manage them
by hand — this table is the reference for what a given CLI generates against.

| Crate | Version | Role |
|---|---|---|
| `forgedb-types` | `0.2` | Core type system (uuid, timestamp, primitives) |
| `forgedb-storage` | `0.2` | Columnar storage facade (positional-I/O columns) |
| `forgedb-wal` | `0.2` | Write-ahead log (opaque `Raw` durable-write path) |
| `forgedb-changefeed` | `0.2` | Change-feed broadcast + durable replication broker |
| `forgedb-auth` | `0.1` | Verify-only JWT + tenant cross-check middleware |
| `forgedb-query-params` | `0.1` | REST query-string → generic filter/sort/paginate |
| `forgedb-compaction` | `0.1` | In-process dead-row reclaim (keep-set GC) |
| `forgedb-txn` | `0.1` | MVCC Tier-2 commit sequencer (LSN + conflict detection) |
| `forgedb-coordinator` | `0.2` | MVCC Tier-3 multi-process write coordinator |

These are schema-agnostic substrate: they know nothing about any specific schema. All
schema-tailored logic (types, tables, queries, filters, relations, API routes) is
*generated* per app at compile time — never reconstructed at runtime by a generic engine.
See [core concepts](/docs/concepts/) for the identity invariant.

<Callout type="note" title="Version lines are independent">
Version pins are intentionally **not** normalized across the substrate — each crate has an
independent version line and is bumped only when it changes.
</Callout>

## Next

Once `forgedb` is on your `PATH`, walk the full loop in the
[quickstart](/docs/quickstart/).
