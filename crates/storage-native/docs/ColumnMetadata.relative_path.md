Path of the column's file relative to the model directory, for example `fixed/uuid_0.bin` or `variable/string_data_1.bin`. Missing from the file means empty.

For a variable column this names the data file; the offsets file is the same path with `_data_` replaced by `_offsets_` (`variable/string_offsets_1.bin`).
