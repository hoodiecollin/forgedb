Totals across every model that [`crate::StatsCollector::collect_database_stats`] could
measure.

A model whose statistics fail to collect is omitted, so the totals cover only the models
listed in `models`.
