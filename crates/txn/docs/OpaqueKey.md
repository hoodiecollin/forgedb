An opaque conflict key: arbitrary bytes identifying one row or one unique-key claim.

The sequencer never interprets the bytes. It compares them only for equality when detecting write-write conflicts.
