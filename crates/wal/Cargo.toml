[package]
name = "forgedb-wal"
version = "0.2.3"
edition.workspace = true
license = "MIT OR Apache-2.0"
description = "Write-Ahead Log (WAL) implementation for ForgeDB"
repository = "https://github.com/hoodiecollin/forgedb"
keywords = ["database", "wal", "forgedb", "durability"]
categories = ["database"]

[dependencies]
crc32fast = "1.3"

[dev-dependencies]
tempfile = "3.8"
