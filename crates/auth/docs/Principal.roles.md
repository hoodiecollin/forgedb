Roles or scopes lifted from the first of `roles`, `permissions`, `scp`,
`scope` that holds an array or a string.

An array contributes its string elements (others are skipped); a string is
split on whitespace. Empty when none of the four is present in either form.
