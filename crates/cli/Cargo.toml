[package]
name = "sinkdb-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "sinkdb"
path = "src/main.rs"

[dependencies]
clap = { version = "4.4", features = ["derive", "cargo"] }
anyhow = "1.0"
thiserror = "1.0"
colored = "2.1"
sinkdb = { path = "../.." }
sinkdb-watcher = { path = "../watcher" }

[dev-dependencies]
tempfile = "3.8"
