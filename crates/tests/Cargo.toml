[package]
name = "forgedb-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
forgedb = { path = "../.." }
forgedb-storage = { path = "../storage" }
forgedb-types = { path = "../types" }
forgedb-validation = { path = "../validation" }
uuid = { version = "1.6", features = ["v4"] }
