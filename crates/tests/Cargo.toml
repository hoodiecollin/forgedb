[package]
name = "sinkdb-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
sinkdb = { path = "../.." }
sinkdb-storage = { path = "../storage" }
sinkdb-types = { path = "../types" }
sinkdb-validation = { path = "../validation" }
uuid = { version = "1.6", features = ["v4"] }
