Another turn stayed outstanding for the whole grant wait and no turn was granted.

The coordinator has already waited up to `min(turn_timeout, client_deadline_ms - 500ms)` before sending this; the client may back off and send `RequestTurn` again.
