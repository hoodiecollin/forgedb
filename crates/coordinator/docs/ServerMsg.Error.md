The request could not be honoured; the reply carries a human-readable message and the connection stays open.

Sent for a [`ClientMsg::Committed`] whose `turn_id` is not the current turn, or when no turn is outstanding (for example after the turn was reclaimed on timeout).
