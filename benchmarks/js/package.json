{
  "name": "forgedb-bench-js",
  "private": true,
  "type": "module",
  "description": "JS/Bun benchmark suite: PGlite (Postgres WASM, in-process) vs bun:sqlite, over the same seeded corpus as the Rust suites. See docs/BENCHMARKS.md.",
  "scripts": {
    "bench": "bun run bench.ts"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "mitata": "^1.0.34"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
