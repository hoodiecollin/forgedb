[package]
name = "inline-string-261"
version = "0.0.0"
edition = "2024"
publish = false
description = "Experiment #261 — inline string(N) slot vs pointer indirection. See the issue for the verdict."

[workspace]

[dependencies]
forgedb-storage = { path = "../../../crates/storage" }
memmap2 = "0.9"
serde_json = "1"

[profile.release]
debug = true
